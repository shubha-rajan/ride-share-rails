            <p>NAME: <%= @driver.name%></p>
            <p>VIN: <%= @driver.vin %></p>
            <p>TRIPS:</p>
            <ul>
            <% @driver.trips.each do |trip| %>
                <li>
                <p>DATE:<%= trip.date %></p>
                <p>RATING:<%= trip.rating %></p>
                <p>COST:<%= trip.cost %></p>
                <p>PASSENGER:<%= link_to trip.passenger.name, passengers_path(trip.passenger.id) %></p>
                </li>
            <%end%>
            </ul>