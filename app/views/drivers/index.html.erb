<h2>BIG LIST OF DRIVERS</h2>
<%=link_to "Add a driver", new_driver_path%>
    <ul>
    <% @drivers.each do |driver|%>
        <li>
            <p>NAME: <%= link_to driver.name, driver_path(driver.id)%></p>
            <p>VIN: <%= driver.vin %></p>
        </li>
    <%end%>
    </ul>
